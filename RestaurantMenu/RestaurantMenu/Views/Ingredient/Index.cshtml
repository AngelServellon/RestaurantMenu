@model IEnumerable<RestaurantMenu.Models.MealIngredient>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2 class="text-center">Ingredients</h2>
<br />
<br />

<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-sm-12 col-md-3">
            <img class="img-responsive" src="@RestaurantMenu.Controllers.IngredientController.IngredientImage(item.id_Ingredient)" alt="Alternate Text" />
            <h3 class="text-center">@Html.ActionLink(@RestaurantMenu.Controllers.IngredientController.IngredientName(item.id_Ingredient), "IngredientMeals", new { id = item.id_Meal })</h3>
        </div>
    }
</div>

